# Data Directory

Place the Lending Club source dataset here as:

- `loan.csv`

The raw dataset is excluded from Git history to keep the repository lightweight and compliant with GitHub file size limits.
